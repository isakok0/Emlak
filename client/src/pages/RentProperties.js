import React from 'react';
import Properties from './Properties';

const RentProperties = () => {
  return <Properties initialFilters={{ listingType: 'rent' }} />;
};

export default RentProperties;


